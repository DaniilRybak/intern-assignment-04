package com.example.intern_assignment_04

import kotlin.test.Test
import kotlin.test.assertEquals

/** Базовые smoke-тесты общего модуля composeApp. */
class ComposeAppCommonTest {

    /** Проверяет, что тестовая инфраструктура commonTest работает корректно. */
    @Test
    fun example() {
        assertEquals(3, 1 + 2)
    }
}
